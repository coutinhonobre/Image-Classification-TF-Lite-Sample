<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/permissionsFragment">

    <fragment
        android:id="@+id/permissionsFragment"
        android:name="com.example.imageclassification.fragments.PermissionsFragment"
        android:label="PermissionsFragment" >
        <action
            android:id="@+id/actionPermissionsToCamera"
            app:destination="@id/cameraFragment"
            app:popUpTo="@id/permissionsFragment"
            app:popUpToInclusive="true"/>
    </fragment>
    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.example.imageclassification.fragments.CameraFragment"
        android:label="CameraFragment" >
        <action
            android:id="@+id/actionCameraToPermissions"
            app:destination="@id/permissionsFragment"
            app:popUpTo="@id/cameraFragment"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>